services:
  - type: web
    name: new-trader-paper-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 scripts/paper_trade_server.py --host 0.0.0.0 --port $PORT --interval-sec 60 --days 1 --warmup-days 10 --market futures_global --entry-mode maker_limit --modules ANKR RIF GALA_73 GALA_10 GALA_112 SPELL
    plan: starter
